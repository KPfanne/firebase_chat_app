import 'dart:ui';

class AppColors {
  // Text
  static const textPrimary = Color(0xFFF5FBFB);
  static const textSecondary = Color(0xFFCFE8E8);

  // Karten / Chat-Bubbles (eigene Nachricht)
  static const bubbleMine = Color(0xFF0A4F4F);
  static const bubbleOther = Color(0xCCFFFFFF);

  // Chat-Liste-Items
  static const glassSurface = Color(0x33FFFFFF);
  static const glassBorder = Color(0x4DFFFFFF);

  // Akzentfarbe
  static const accent = Color(0xFFFF8C61);
  static const accentSoft = Color(0xFFFFB199);
}
